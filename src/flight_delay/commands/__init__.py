"""Pipeline steps, each reachable from the command line.

Every number in `docs/` is produced by one of these. Keeping them here rather
than in a notebook or a scratch script is what makes the documentation
checkable: a reader who doubts a figure can run the command that made it.
"""
